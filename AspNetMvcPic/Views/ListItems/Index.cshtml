@model IEnumerable<AspNetMvcPic.Models.ListItem>

@{
    ViewData["Title"] = "Index";
}

<h1>List of Items</h1>

<p>
    <a class="btn btn-primary" asp-action="Create">Create New</a>
</p>


<!-- 

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ProfilePicture)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                <img src="~/images/@item.ProfilePicture"
                     class="card"
                     height="340" width="340"
                     asp-append-version="true" />
                @item.Name
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                <a class="btn btn-warning" asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a class="btn btn-info" asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a class="btn btn-danger" asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
-->


<div class="row">
    @foreach (var i in Model)
    {
        <div class="col-md-6">
            <img src="~/images/@i.ProfilePicture"
                 class="card"
                 height="340" width="340"
                 asp-append-version="true" />
            @i.Name
            <div class="col-md-6">
                <a class="btn btn-warning" asp-action="Edit" asp-route-id="@i.Id">Edit</a> |
                <a class="btn btn-info" asp-action="Details" asp-route-id="@i.Id">Details</a> |
                <a class="btn btn-danger" asp-action="Delete" asp-route-id="@i.Id">Delete</a>
            </div>

        </div>

    }
</div>